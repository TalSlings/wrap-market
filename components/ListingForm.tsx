"use client";
import{useEffect,useState}from"react";import{createClient}from"@/lib/supabase/client";import{SIZES,GSM,CONDITIONS,DEFECTS,COLOR_PATTERNS}from"@/lib/constants";import{sanitizeImage}from"@/lib/image";
export default function ListingForm({userId,manufacturers,materials,colors,regions,subregions,initial}:{[k:string]:any}){const s=createClient();const[manufacturerId,setManufacturerId]=useState(initial?.manufacturer_id||"");const[newManufacturer,setNewManufacturer]=useState("");const[design,setDesign]=useState(initial?.design||"");const[model,setModel]=useState(initial?.model||"");const[price,setPrice]=useState(String(initial?.price||""));const[description,setDescription]=useState(initial?.description||"");const[size,setSize]=useState(initial?.size||"");const[sizeNote,setSizeNote]=useState(initial?.size_note||"");const[gsm,setGsm]=useState(initial?.gsm||"unknown");const[condition,setCondition]=useState(initial?.condition||"");const[defects,setDefects]=useState<string[]>(initial?.defects||["none"]);const[defDesc,setDefDesc]=useState(initial?.defects_description||"");const[shipping,setShipping]=useState(initial?.shipping_available??true);const[moreInfo,setMoreInfo]=useState(initial?.more_info_url||"");const[selectedColors,setSelectedColors]=useState<string[]>(initial?.colors||[]);const[patterns,setPatterns]=useState<string[]>(initial?.color_patterns||[]);const[mats,setMats]=useState<any[]>(initial?.materials?.map((x:any)=>({material_id:x.material_id,percentage:String(x.percentage)}))||[{material_id:"",percentage:""}]);const[regionIds,setRegionIds]=useState<string[]>(initial?.locations?.map((x:any)=>x.region_id)||[]);const[subIds,setSubIds]=useState<string[]>(initial?.locations?.map((x:any)=>x.subregion_id).filter(Boolean)||[]);const[mainImages,setMainImages]=useState<File[]>([]);const[defectImages,setDefectImages]=useState<File[]>([]);const[msg,setMsg]=useState("");const[busy,setBusy]=useState(false);const toggle=(a:string[],v:string)=>a.includes(v)?a.filter(x=>x!==v):[...a,v];
useEffect(()=>{if(initial)return;try{const x=JSON.parse(sessionStorage.getItem("wrap-market-last-publish")||"{}");if(x.shipping!==undefined)setShipping(x.shipping);if(x.regionIds)setRegionIds(x.regionIds);if(x.subIds)setSubIds(x.subIds)}catch{}},[initial]);
const toggleDef=(v:string)=>{if(v==='none')setDefects(['none']);else setDefects(toggle(defects.filter(x=>x!=='none'),v))};
async function ensureManufacturer(){if(manufacturerId)return manufacturerId;if(!newManufacturer.trim())throw new Error('צריך לבחור או להוסיף יצרן');const{data,error}=await s.from('manufacturers').insert({name:newManufacturer.trim(),created_by:userId}).select('id').single();if(error)throw error;return data.id}
async function upload(id:string,files:File[],kind:'listing'|'defect'){for(let i=0;i<files.length;i++){const blob=await sanitizeImage(files[i]);const path=`${userId}/${id}/${kind}-${Date.now()}-${i}.jpg`;const{error}=await s.storage.from('listing-images').upload(path,blob,{contentType:'image/jpeg'});if(error)throw error;const{error:e}=await s.from('listing_images').insert({listing_id:id,owner_id:userId,storage_path:path,image_type:kind,position:i});if(e)throw e}}
async function save(status:'draft'|'active'){setBusy(true);setMsg('');try{const mid=await ensureManufacturer();const sum=mats.filter(x=>x.material_id).reduce((a,x)=>a+Number(x.percentage||0),0);if(status==='active'){if(!design||!price||!size||!condition)throw new Error('חסרים שדות חובה');if(!initial&&mainImages.length<1)throw new Error('צריך לפחות תמונה אחת');if(!mats.some(x=>x.material_id)||Math.abs(sum-100)>.001)throw new Error('הרכב החומרים צריך להסתכם ב־100%');if(!regionIds.length)throw new Error('צריך לבחור לפחות אזור אחד')}const payload={owner_id:userId,manufacturer_id:mid,design,model:model||null,price:Number(price||0),description:description||null,size,size_note:sizeNote||null,gsm,condition,defects,defects_description:defDesc||null,shipping_available:shipping,more_info_url:moreInfo||null,colors:selectedColors,color_patterns:patterns,status};let id=initial?.id;if(id){const{error}=await s.from('listings').update(payload).eq('id',id);if(error)throw error;await s.from('listing_materials').delete().eq('listing_id',id);await s.from('listing_locations').delete().eq('listing_id',id)}else{const{data,error}=await s.from('listings').insert(payload).select('id').single();if(error)throw error;id=data.id}const mr=mats.filter(x=>x.material_id).map((x,i)=>({listing_id:id,material_id:x.material_id,percentage:Number(x.percentage),position:i}));if(mr.length){const{error}=await s.from('listing_materials').insert(mr);if(error)throw error}const lr=[...regionIds.map(r=>({listing_id:id,region_id:r,subregion_id:null})),...subIds.map(si=>({listing_id:id,region_id:subregions.find((x:any)=>x.id===si).region_id,subregion_id:si}))];if(lr.length){const{error}=await s.from('listing_locations').insert(lr);if(error)throw error}if(mainImages.length)await upload(id,mainImages,'listing');if(defectImages.length)await upload(id,defectImages,'defect');sessionStorage.setItem('wrap-market-last-publish',JSON.stringify({shipping,regionIds,subIds}));location.href=status==='active'?`/listing/${id}`:'/account'}catch(e:any){setMsg(e.message||String(e));setBusy(false)}}
return <div><div className="section"><h2>זהות המנשא</h2><div className="field"><label>יצרן ⓘ</label><select className="select" value={manufacturerId} onChange={e=>setManufacturerId(e.target.value)}><option value="">בחרי יצרן</option>{manufacturers.map((m:any)=><option key={m.id} value={m.id}>{m.name}</option>)}</select>{!manufacturerId&&<input className="input" value={newManufacturer} onChange={e=>setNewManufacturer(e.target.value)} placeholder="לא מצאת? הוסיפי יצרן חדש"/>}</div><div className="field"><label>עיצוב ⓘ</label><input className="input" value={design} onChange={e=>setDesign(e.target.value)}/></div><div className="field"><label>מודל ⓘ</label><input className="input" value={model} onChange={e=>setModel(e.target.value)}/></div><div className="field"><label>מידה ⓘ</label><select className="select" value={size} onChange={e=>setSize(e.target.value)}><option value="">בחרי</option>{SIZES.map(([k,l])=><option key={k} value={k}>{l}</option>)}</select></div><div className="field"><label>הערת מידה</label><input className="input" value={sizeNote} onChange={e=>setSizeNote(e.target.value)}/></div><div className="field"><label>GSM ⓘ</label><select className="select" value={gsm} onChange={e=>setGsm(e.target.value)}>{GSM.map(([k,l])=><option key={k} value={k}>{l}</option>)}</select></div></div>
<div className="section"><h2>חומרים וצבע</h2>{mats.map((r,i)=><div className="material-row" key={i}><select className="select" value={r.material_id} onChange={e=>{const n=[...mats];n[i]={...n[i],material_id:e.target.value};setMats(n)}}><option value="">חומר</option>{materials.map((m:any)=><option key={m.id} value={m.id}>{m.name}</option>)}</select><input className="input" style={{width:90}} inputMode="decimal" placeholder="%" value={r.percentage} onChange={e=>{const n=[...mats];n[i]={...n[i],percentage:e.target.value};setMats(n)}}/></div>)}{mats.length<10&&<button className="btn" onClick={()=>setMats([...mats,{material_id:'',percentage:''}])}>＋ חומר</button>}<div className="field"><label>צבעים</label><div className="chips">{colors.map((c:any)=><button key={c.key} className={'chip '+(selectedColors.includes(c.key)?'active':'')} onClick={()=>setSelectedColors(toggle(selectedColors,c.key))}><span className="swatch" style={{display:'inline-block',background:c.hex}}/> {c.label}</button>)}</div></div><div className="field"><label>מבנה הצביעה</label><div className="chips">{COLOR_PATTERNS.map(([k,l])=><button key={k} className={'chip '+(patterns.includes(k)?'active':'')} onClick={()=>setPatterns(toggle(patterns,k))}>{l}</button>)}</div></div></div>
<div className="section"><h2>מצב</h2><div className="field"><label>מצב</label><select className="select" value={condition} onChange={e=>setCondition(e.target.value)}><option value="">בחרי</option>{CONDITIONS.map(([k,l])=><option key={k} value={k}>{l}</option>)}</select></div><div className="field"><label>פגמים</label><div className="chips">{DEFECTS.map(([k,l])=><button key={k} className={'chip '+(defects.includes(k)?'active':'')} onClick={()=>toggleDef(k)}>{l}</button>)}</div></div>{!defects.includes('none')&&<div className="field"><label>תיאור פגמים</label><textarea className="textarea" value={defDesc} onChange={e=>setDefDesc(e.target.value)}/></div>}</div>
<div className="section"><h2>מכירה ומסירה</h2><div className="field"><label>מחיר</label><input className="input" inputMode="numeric" value={price} onChange={e=>setPrice(e.target.value)}/></div><div className="field"><label>תיאור</label><textarea className="textarea" value={description} onChange={e=>setDescription(e.target.value)}/></div><div className="field"><label><input type="checkbox" checked={shipping} onChange={e=>setShipping(e.target.checked)}/> משלוח זמין ⓘ</label><div className="notice">דמי משלוח על חשבון הקונה אלא אם צוין אחרת.</div></div><div className="field"><label>אזורים</label><div className="chips">{regions.map((r:any)=><button key={r.id} className={'chip '+(regionIds.includes(r.id)?'active':'')} onClick={()=>setRegionIds(toggle(regionIds,r.id))}>{r.name}</button>)}</div></div>{regionIds.map(r=><div className="field" key={r}><label>תתי־אזורים — {regions.find((x:any)=>x.id===r)?.name}</label><div className="chips">{subregions.filter((x:any)=>x.region_id===r).map((si:any)=><button key={si.id} className={'chip '+(subIds.includes(si.id)?'active':'')} onClick={()=>setSubIds(toggle(subIds,si.id))}>{si.name}</button>)}</div></div>)}<div className="field"><label>קישור למידע נוסף</label><input className="input" type="url" value={moreInfo} onChange={e=>setMoreInfo(e.target.value)} placeholder="WrapTrack / אתר היצרן"/></div></div>
<div className="section"><h2>תמונות</h2><div className="notice">יש אנשים בתמונה? מומלץ לכסות או לטשטש פנים, במיוחד של ילדים, לפני ההעלאה.</div><div className="field"><label>תמונות ראשיות — עד 9</label><input className="input" type="file" accept="image/*" multiple onChange={e=>setMainImages(Array.from(e.target.files||[]).slice(0,9))}/></div><div className="field"><label>תמונות פגמים — עד 4</label><input className="input" type="file" accept="image/*" multiple onChange={e=>setDefectImages(Array.from(e.target.files||[]).slice(0,4))}/></div></div>{msg&&<p className="danger">{msg}</p>}<div className="toolbar"><button className="btn" disabled={busy} onClick={()=>save('draft')}>שמירת טיוטה</button><button className="btn primary" disabled={busy} onClick={()=>save('active')}>{busy?'שומרת...':'פרסום מודעה'}</button></div></div>}
