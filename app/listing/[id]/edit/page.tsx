import{redirect,notFound}from"next/navigation";import{createClient}from"@/lib/supabase/server";import ListingForm from"@/components/ListingForm";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect(`/login?next=/listing/${id}/edit`);const[{data:l},{data:manufacturers},{data:materials},{data:colors},{data:regions},{data:subregions}]=await Promise.all([s.from('listings').select('*,materials:listing_materials(material_id,percentage),locations:listing_locations(region_id,subregion_id)').eq('id',id).eq('owner_id',user.id).single(),s.from('manufacturers').select('id,name').eq('status','active').order('name'),s.from('materials').select('id,name,parent_material_id,vegan,material_origin').eq('status','active').order('name'),s.from('colors').select('*').eq('active',true).order('sort_order'),s.from('regions').select('*').eq('active',true).order('sort_order'),s.from('subregions').select('*').eq('active',true).order('sort_order')]);if(!l)notFound();return <main className="page"><h1>עריכת מודעה</h1><ListingForm initial={l} userId={user.id} manufacturers={manufacturers||[]} materials={materials||[]} colors={colors||[]} regions={regions||[]} subregions={subregions||[]}/></main>}
