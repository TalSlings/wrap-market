import{notFound}from"next/navigation";import{createClient}from"@/lib/supabase/server";import{SIZES,GSM,CONDITIONS,labelOf}from"@/lib/constants";import ViewCounter from"@/components/ViewCounter";import ContactBox from"@/components/ContactBox";export const dynamic="force-dynamic";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const s=await createClient();const{data:{user}}=await s.auth.getUser();const{data:l}=await s.from("listings").select(`*,manufacturer:manufacturers(name),materials:listing_materials(percentage,material:materials(name,vegan,material_origin)),locations:listing_locations(region:regions(name),subregion:subregions(name)),images:listing_images(storage_path,image_type,position)`).eq("id",id).single();if(!l)notFound();const urls:any[]=[];for(const im of(l.images||[])){const{data}=await s.storage.from("listing-images").createSignedUrl(im.storage_path,3600);urls.push({...im,url:data?.signedUrl})}const main=urls.filter(x=>x.image_type==='listing').sort((a,b)=>a.position-b.position);const defects=urls.filter(x=>x.image_type==='defect');return <main className="page"><ViewCounter id={id}/><div className="section">{main[0]?.url&&<img className="gallery-main" src={main[0].url} alt=""/>}<div className="thumbrow">{main.slice(1).map((x,i)=><img className="thumb" key={i} src={x.url} alt=""/>)}</div></div><div className="section"><h1>{l.manufacturer?.name}</h1><div className="design">{l.design}</div>{l.model&&<div className="model">{l.model}</div>}<p>{labelOf(SIZES,l.size)} {l.size_note&&`· ${l.size_note}`}</p><p>{l.price} ₪</p><div className="details"><div><b>GSM</b><br/>{labelOf(GSM,l.gsm)}</div><div><b>מצב</b><br/>{labelOf(CONDITIONS,l.condition)}</div></div></div><div className="section"><h2>הרכב</h2>{(l.materials||[]).map((x:any,i:number)=><p key={i}>{x.percentage}% {x.material?.name}</p>)}</div><div className="section"><h2>מסירה</h2><p>📍 {Array.from(new Set((l.locations||[]).map((x:any)=>x.region?.name).filter(Boolean))).join(', ')}</p><p>{l.shipping_available?'🚚 משלוח זמין':'ללא משלוח'}</p>{l.shipping_available&&<p className="notice">דמי משלוח על חשבון הקונה אלא אם צוין אחרת.</p>}</div>{l.description&&<div className="section"><h2>תיאור</h2><p>{l.description}</p></div>}{l.more_info_url&&<div className="section"><a target="_blank" rel="noopener noreferrer" href={l.more_info_url}>מידע נוסף על המנשא ↗</a></div>}<ContactBox listingId={id} userId={user?.id}/><details className="section"><summary><b>מידע על פגמים</b></summary><p>{(l.defects||[]).includes('none')?'לא צוינו פגמים ידועים':l.defects_description||'צוינו פגמים'}</p><div className="thumbrow">{defects.map((x,i)=><img className="thumb" key={i} src={x.url} alt=""/>)}</div></details></main>}
