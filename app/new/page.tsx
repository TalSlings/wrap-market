import{redirect}from"next/navigation";import{createClient}from"@/lib/supabase/server";import ListingForm from"@/components/ListingForm";export const dynamic="force-dynamic";export default async function Page(){const s=await createClient();const{data:{user}}=await s.auth.getUser();if(!user)redirect('/login?next=/new');const[{data:manufacturers},{data:materials},{data:colors},{data:regions},{data:subregions}]=await Promise.all([s.from('manufacturers').select('id,name').eq('status','active').order('name'),s.from('materials').select('id,name,parent_material_id,vegan,material_origin').eq('status','active').order('name'),s.from('colors').select('*').eq('active',true).order('sort_order'),s.from('regions').select('*').eq('active',true).order('sort_order'),s.from('subregions').select('*').eq('active',true).order('sort_order')]);return <main className="page"><h1>פרסום מנשא</h1><ListingForm userId={user.id} manufacturers={manufacturers||[]} materials={materials||[]} colors={colors||[]} regions={regions||[]} subregions={subregions||[]}/></main>}
