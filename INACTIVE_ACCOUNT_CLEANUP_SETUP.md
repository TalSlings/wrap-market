# הפעלת מחיקת חשבונות לא פעילים

הקוד מוכן, אבל המחיקה לא תפעל עד להשלמת כל השלבים במסמך זה.

## המדיניות שמיושמת

- הפעילות נקבעת לפי מועד ההתחברות האחרון ששמור ב-Supabase Auth.
- חשבון ללא היסטוריית מודעות: מחיקה אחרי 18 חודשים והתראה 30 יום לפני.
- חשבון עם היסטוריית מודעות: מחיקה אחרי 30 חודשים והתראות 30, 7, 3 ויום לפני.
- התחברות מוצלחת מבטלת את המחיקה המתוכננת.
- חשבונות עם תפקיד `admin` או `instructor` אינם נמחקים אוטומטית.
- בהפעלה הראשונה, חשבון שכבר הגיע לסף מקבל 30 ימי חסד ואינו נמחק מיד.
- לפני מחיקת החשבון נמחקות תמונות המודעות מהאחסון.
- לאחר המחיקה נשלחת הודעה שמזמינה את המשתמשת להירשם מחדש בכל עת.

## 1. בסיס הנתונים

ב-Supabase SQL Editor יש להריץ את:

`supabase/migrations/003_inactive_account_cleanup.sql`

הקובץ יוצר רק טבלת מעקב חדשה ואינו מוחק נתונים.

## 2. שירות שליחת מיילים

המנגנון משתמש ב-Resend. יש לפתוח חשבון, לאמת כתובת או דומיין שולח ולקבל API key.

## 3. משתני סביבה ב-Vercel

יש להוסיף לכל סביבת Production:

- `SUPABASE_SERVICE_ROLE_KEY` — מתוך Supabase, לשימוש בצד השרת בלבד.
- `CRON_SECRET` — מחרוזת אקראית ארוכה.
- `RESEND_API_KEY` — המפתח משירות המיילים.
- `CLEANUP_FROM_EMAIL` — כתובת שולחת מאומתת, כולל שם אם רוצים.

אין להוסיף את הקידומת `NEXT_PUBLIC_` לאף אחד מארבעת המשתנים האלה.

## 4. פריסה

הקובץ `vercel.json` מפעיל את הבדיקה פעם ביום. לאחר העלאת הקבצים והוספת
משתני הסביבה יש לבצע deployment חדש.

## 5. בדיקה לפני הפעלה אמיתית

לפני שמשאירים את המשימה פעילה יש ליצור חשבון בדיקה ולוודא:

1. שהמשימה היומית מזוהה ב-Vercel.
2. שהודעות נשלחות מהכתובת הנכונה.
3. שהתחברות מבטלת רשומת מחיקה מתוכננת.
4. שתמונות של חשבון בדיקה נמחקות מה-Storage.
5. שחשבון בדיקה נמחק מ-Supabase Auth.

אין לקצר ידנית את התאריכים של חשבונות אמיתיים לצורך הבדיקה.
